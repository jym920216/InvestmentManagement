app:
  version: "%%VERSION%%"
      
server:
  port: 10100
eureka:
  instance:
    hostname: discovery-service
    preferIpAddress: true
    ipAddress: 112.74.173.112
  server:
    enableSelfPreservation: false
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
